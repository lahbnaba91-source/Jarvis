# Copy this file to .env.local (already gitignored) and fill in your own token.
# Never commit real tokens.

# Free Cesium ion account -> https://ion.cesium.com/signup
# Access Tokens tab -> "Default" token (or create one scoped to this app).
# Used for Cesium World Terrain + Cesium OSM Buildings (both free-tier assets).
NEXT_PUBLIC_CESIUM_ION_TOKEN=
