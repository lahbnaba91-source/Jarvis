[project]
name = "jarvis-voice"
version = "0.1.0"
description = "Browser-mic voice line for Jarvis, wired to the ai-visualizer face."
requires-python = ">=3.11,<3.13"
dependencies = [
    "claude-agent-sdk>=0.2.100",
    "faster-whisper>=1.0.0",
    "kokoro>=0.9.0",
    "numpy>=1.26.0",
    "aiohttp>=3.9.0",
]

[tool.uv]
package = false
